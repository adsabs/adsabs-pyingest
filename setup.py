from setuptools import setup

setup(
    name='pyingest',
    version='0.5.0',
    packages=['pyingest','pyingest.config','pyingest.extractors','pyingest.parsers','pyingest.serializers','pyingest.validators'],
)
